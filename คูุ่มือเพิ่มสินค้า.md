# วิธีเพิ่มสินค้าใหม่ในเว็บไซต์ ARIWA (แบบเข้าใจง่าย)

ไฟล์เดียวที่ต้องแก้เกือบทุกครั้งคือ **`products.js`**
ไฟล์นี้เก็บข้อมูลสินค้าทั้งหมดของเว็บ พอแก้ไฟล์นี้แล้วบันทึก เว็บจะอัปเดตให้อัตโนมัติ ไม่ต้องไปแก้หน้าอื่น

---

## กรณีที่ 1: เพิ่ม "สินค้ารุ่นใหม่" ในแบรนด์ที่มีอยู่แล้ว

ตัวอย่าง: อยากเพิ่มกล้องรุ่น **N70** เข้าไปในแบรนด์ **Kiloview** ที่มีอยู่แล้ว

### ขั้นตอน

1. **เตรียมรูปสินค้า** เอาไฟล์รูปไปวางในโฟลเดอร์ของแบรนด์นั้น เช่น

   ```
   assets/products/Kiloview/N70.png
   ```

2. **เปิดไฟล์ `products.js`** หาแบรนด์ Kiloview แล้วเพิ่มบรรทัดใหม่เข้าไปใน `products: [ ]`

   ```js
   {
     name: "Kiloview",
     type: "ip",
     ...
     products: [
       { name: "N60", image: "assets/products/Kiloview/N60.png" },
       { name: "N50", image: "assets/products/Kiloview/N50.png" },

       // 👇 เพิ่มบรรทัดนี้เข้าไปใหม่
       { name: "N70", image: "assets/products/Kiloview/N70.png" },

       { name: "P3", image: "assets/products/Kiloview/P3.png" },
       { name: "E3", image: "assets/products/Kiloview/E3.png" },
     ],
   },
   ```

3. **บันทึกไฟล์** กด ctrl + s สินค้าใหม่จะไปโผล่ในหน้ารายละเอียดของ Kiloview เอง วิธีเช็คว่าเพิ่มไหม ไปที่ไฟล์ index.html กดคลิกขวา เเล้วกด open with five server แล้วเข้าไปดูที่หน้าเว็บว่าถูกเพิ่มไหม

> ⚠️ **ห้ามลืมเครื่องหมายจุลภาค (`,`)** ต่อท้ายแต่ละบรรทัด/แต่ละ `{ }` ไม่งั้นเว็บจะพังทั้งหน้า

---

## กรณีที่ 2: เพิ่ม "แบรนด์ใหม่" ทั้งแบรนด์

ตัวอย่าง: อยากเพิ่มแบรนด์ใหม่ชื่อ **ABC Broadcast**

### ขั้นตอน

1. **เตรียมไฟล์รูป**
   - โลโก้แบรนด์ → วางที่ `assets/logos/abc.png`
   - สร้างโฟลเดอร์เก็บรูปสินค้า → `assets/products/ABC/`

2. **เปิด `products.js`** แล้วเพิ่มก้อนข้อมูลใหม่นี้ **ต่อท้ายรายการสุดท้าย** (ก่อนวงเล็บปิด `];` ท้ายไฟล์)

   ```js
   {
     name: "ABC Broadcast",              // ชื่อแบรนด์ (ห้ามซ้ำกับแบรนด์อื่น)
     type: "broadcast",                  // เลือกได้: "broadcast" | "ip" | "ai"
     desc: "คำอธิบายสั้น ๆ เกี่ยวกับแบรนด์นี้",
     url: "https://www.abc-broadcast.com/",
     logo: "assets/logos/abc.png",

     products: [
       { name: "ชื่อสินค้า 1", image: "assets/products/ABC/product1.png" },
       { name: "ชื่อสินค้า 2", image: "assets/products/ABC/product2.png" },
     ],
   },
   ```

3. **บันทึกไฟล์** กด ctrl + s แบรนด์ใหม่จะขึ้นเป็นการ์ดในหน้า "Products" เอง และคลิกเข้าไปดูรายละเอียดได้ทันที ไม่ต้องแก้ไฟล์อื่นเพิ่ม

---

## เช็คลิสต์ก่อนอัปโหลดขึ้นเว็บจริง

- [ ] ชื่อไฟล์รูป path ตรงกับไฟล์จริง (ตัวพิมพ์เล็ก/ใหญ่มีผล)
- [ ] มีจุลภาค `,` คั่นระหว่างแต่ละ object ครบ
- [ ] ชื่อแบรนด์ (`name`) ไม่ซ้ำกับที่มีอยู่แล้ว
- [ ] เปิดเว็บทดสอบดูก่อนอัปขึ้นจริง
- [ ] สำรอง (copy) ไฟล์ `products.js` เดิมเก็บไว้ก่อนแก้ทุกครั้ง
      วิธีอัปขึ้นเว็บจริง (Push ขึ้น GitHub → Netlify deploy อัตโนมัติ)

หลังจากแก้ไฟล์และเช็คด้วย Live Server แล้วว่าถูกต้อง ขั้นตอนสุดท้ายคือส่งงานขึ้นเว็บจริงผ่าน VS Code:

เปิด Source Control panel กด Ctrl + Shift + G (หรือคลิกไอคอนรูปกิ่งไม้แยกที่แถบซ้ายสุด ที่มีเลขแดง/น้ำเงินบอกจำนวนไฟล์) จะเห็นรายชื่อไฟล์ทั้งหมดที่แก้ไปแล้ว เช่น style.css, product-detail.html
ตรวจสอบว่าไฟล์ไหนถูกแก้บ้าง คลิกที่ชื่อไฟล์แต่ละอันในลิสต์ VS Code จะโชว์ diff (เขียว = เพิ่ม, แดง = ลบ) เทียบกับเวอร์ชันเดิมใน Git ให้เช็คว่าเป็นการแก้ที่ตั้งใจจริง ๆ ก่อน push
Stage การเปลี่ยนแปลง เอาเมาส์ไปชี้ที่คำว่า "Changes" (หัวข้อรวมไฟล์) แล้วกดเครื่องหมาย + ที่โผล่ขึ้นมา เพื่อ stage ทุกไฟล์พร้อมกัน (หรือกด + ทีละไฟล์ถ้าอยากเลือกเฉพาะบางไฟล์)
ใส่ commit message พิมพ์ข้อความสั้น ๆ อธิบายว่าแก้อะไร ในช่องข้อความด้านบน เช่น Add StreamPro brand หรือ Fix slider button overflow on desktop — ข้อความนี้จะช่วยให้ย้อนดูประวัติทีหลังได้ง่าย
กด Commit กดปุ่ม ✓ Commit (หรือ Ctrl + Enter) เพื่อบันทึกการเปลี่ยนแปลงเข้า Git แบบ local ก่อน — ขั้นนี้ยังไม่ได้ขึ้น GitHub นะครับ แค่บันทึกในเครื่องตัวเอง
กด Sync/Push ขึ้น GitHub หลัง commit เสร็จ ปุ่มจะเปลี่ยนเป็น "Sync Changes" หรือไอคอนลูกศรหมุนพร้อมตัวเลข กดปุ่มนั้นเพื่อ push commit ขึ้น GitHub จริง ๆ (ถ้าเป็นครั้งแรกอาจมีป็อปอัพให้ login GitHub ยืนยันตัวตน)
เช็คผลบน GitHub และ Netlify เข้า github.com/irawansrasuphan/ariwa-website เพื่อดูว่า commit ขึ้นไปแล้ว จากนั้น Netlify จะ deploy ให้อัตโนมัติภายในไม่กี่นาที (เพราะต่อ auto-deploy จาก GitHub ไว้แล้ว) เข้า ariwa.co เช็คได้เลยว่าเว็บอัปเดตหรือยัง
